type Mutation {
    novoUsuario(
        # nome: String,
        # email: String,
        # idade: Int
        
        #Trocando pelo Input
        dados: UsuarioInput!
    ): Usuario!
    
    # excluirUsuario(id: Int!): Usuario
    #Alterando para Input UsuarioFiltro
    excluirUsuario(
        filtro: UsuarioFiltro!
    ): Usuario

    alterarUsuario(
        id: Int!
        nome: String
        email: String
        idade: Int
    ): Usuario
}